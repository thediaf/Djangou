{% extends 'base.html.twig' %}

{% block title %}Djangoud{% endblock %}

{% block body %}

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="#">Djangoud</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {# 
        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Features</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Pricing</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">About</a>
            </li>
            </ul>
            <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="text" placeholder="Search">
            <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </form> #}
        </div>
    </nav>
    <div class="container mt-4">
        {# <div class="form-group">
        <fieldset disabled="">
            <label class="control-label" for="disabledInput">Disabled input</label>
            <input class="form-control" id="disabledInput" type="text" placeholder="" disabled="" witdh="60%" style="height:79;">
        </fieldset>
        </div> #}

        {# <div class="form-group">
        <fieldset>
            <label class="control-label" for="readOnlyInput">Readonly input</label>
            <input class="form-control" id="readOnlyInput" type="text" placeholder="Readonly input here…" readonly="">
        </fieldset>
        </div> #}

            {{form_start(form)}}
                <div class="form-row">
                    <div class="col">
                        {{form_row(form.wordLanguage)}}
                    </div>
                    <div class="col">
                        {{form_row(form.translateLanguage)}}
                    </div>
                </div>
                <div class="form-row">
                    <div class="col mt-3">{{form_row(form.word)}}<button class="btn btn-primary">Chercher</button></div>
                    {# <div class="col">{{form_rest(form)}}</div> #}
                </div>
            
         {{form_end(form)}}
        <div class="row">
            <div id="results">
                <div class="def hide">Définition: <span class="data"></span></div>
                <div class="classe hide">Classe: <span class="data"></span></div>
                <div class="undefined hide text-danger">Définition introuvable</div>
            </div>
        {# {% if wordTranslate is defined and wordTranslate %}
            {% for  in wordTranslate %}
                
            {% endfor %}
            {{wordTranslate.word}}    

        {% endif %} #}
            {# {% for language in languages %} #}
                {# <div class="col-md-4">{{wordTranslate.word}}</div> #}
                {# {% for translate in language.translates %}
                    <div class="col-md-4">{{translate.word}}</div>    
                {% endfor %} #}
                
            {# {% endfor %} #}
        </div>
    </div>
{% endblock %}
